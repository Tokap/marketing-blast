<%= form_for(@outreach) do |f| %>
  <% if @outreach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outreach.errors.count, "error") %> prohibited this outreach from being saved:</h2>

      <ul>
      <% @outreach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-success bl-border" %>
  </div>
<% end %>
